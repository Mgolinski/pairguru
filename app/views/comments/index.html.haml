%h1 Listing comments

%table
  %thead
    %tr
      %th Body
      %th User
      %th Movie
      %th
      %th
      %th

  %tbody
    - @comments.each do |comment|
      %tr
        %td= comment.body
        %td= comment.user
        %td= comment.movie

%br
